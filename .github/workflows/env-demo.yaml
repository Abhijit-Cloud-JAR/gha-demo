name: ENV Demo
on: push
env:
  APP_ENV: "DEV"
jobs:
  env_demo:
    env:
      APP_ENV: "TEST"
    runs-on: ubuntu-latest
    steps:
      - name: Check the Environment
        run: echo "App is connected to $APP_ENV"